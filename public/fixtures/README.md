Bundled CubiCasa dev pair (must share the same pixel dimensions as the JSON `image` field):

- `cubicasa5k-sample.jpg` — 914×1720 inference canvas
- JSON: `src/lib/project/fixtures/cubicasa5k-sample.detections.json`

Use **Load sample plan** on `/project/new` (development). Replace the JPEG with your exact Roboflow Universe upload for pixel-perfect QA.
