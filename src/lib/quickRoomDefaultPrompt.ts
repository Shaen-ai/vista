// Substituted when the Quick Room style textarea is left empty — the field is
// optional in the UI, but the brief/render pipeline always needs a prompt.
export const DEFAULT_QUICK_ROOM_PROMPT =
  "Redesign this room in the selected style so it looks beautiful, cohesive, and functional.";
